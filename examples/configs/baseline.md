# Baseline config notes

This starter repo currently uses an `echo_system` in the CLI so the sample dataset passes deterministically.

Planned next step:
- replace `echo_system` with provider adapters
- add prompt templates
- add agent traces and tool-call scoring
